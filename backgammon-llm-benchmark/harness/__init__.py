"""LLM evaluation harness for the backgammon benchmark.

Async batch runner that evaluates (model x position x track) decisions against
stored rollout ground truth and emits ``results/*.json`` (PLAN.md §5.2).
Phase 0: package skeleton only, no implementation.
"""

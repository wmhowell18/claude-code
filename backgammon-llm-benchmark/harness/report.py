"""Results emitter (PLAN.md §5.2, §6).

Serializes aggregated run metrics and per-decision records into
``results/*.json`` (validated against schema/results.schema.json) for the
static leaderboard site to consume.
"""

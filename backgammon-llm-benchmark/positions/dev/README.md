# positions/dev/

Public dev split (~150 positions), **openly published and non-authoritative**
(PLAN.md §2.5, §3.2). For prompt engineering, harness testing, and repro demos;
assume it will eventually be ingested — treat its scores as non-authoritative
over time. Records validate against `schema/position.schema.json`. Empty in Phase 0.

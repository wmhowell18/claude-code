# data/manifests/

Dataset manifests: per-release content hash + creation date + canary token
(PLAN.md §2.5). The date stamp turns "trust me" into a checkable claim — a model
whose training cutoff predates the creation date provably could not have trained
on the set. Committed. Empty in Phase 0.

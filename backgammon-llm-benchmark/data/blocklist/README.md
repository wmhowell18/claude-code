# data/blocklist/

Versioned dedup blocklist: normalized XGID hashes of **known public** positions
(GNU BG example DBs, public match archives scraped *only to exclude*, opening
books). Candidates matching the blocklist are rejected (PLAN.md §2.3). Committed
so exclusions are reproducible. Empty in Phase 0.

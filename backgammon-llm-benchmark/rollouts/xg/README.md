# rollouts/xg/

**Authoritative** ground-truth rollout data from eXtreme Gammon (PLAN.md §1.3,
§9), keyed by position id. Kept side-by-side with the GNU BG interim rollouts so
they can be compared and swapped in for the authoritative set. Records validate
against `schema/rollout.schema.json`. Empty in Phase 0.

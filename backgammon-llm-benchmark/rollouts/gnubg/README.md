# rollouts/gnubg/

**Authoritative** ground-truth rollout data from GNU BG (PLAN.md §1.3, §7, §9),
keyed by position id. GNU BG is the permanent ground-truth engine (2026-07-15
decision, PLAN.md §9.1) — not an interim stand-in. Records validate against
`schema/rollout.schema.json`. The sibling `rollouts/xg/` is reserved for an
optional future cross-check only and is unused in v1. Empty in Phase 0.

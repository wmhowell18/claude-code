# rollouts/gnubg/

**Interim** ground-truth rollout data from GNU BG (PLAN.md §1.3, §7, §9),
keyed by position id. Used to unblock Phases 0-1 while XG automation is unsolved.
Records validate against `schema/rollout.schema.json`. Empty in Phase 0.

"""GNU BG Position ID + Match ID <-> board_json (PLAN.md §1.1).

Parses/serializes the secondary GNU BG ID pair for interop with GNU BG (our
interim rollout engine). Enables round-trip with ``board_json`` and cross-checks
against the XGID.
"""
